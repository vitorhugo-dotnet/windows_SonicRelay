<Window
    x:Class="SonicRelay.Windows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="SonicRelay Windows Publisher">
  <Grid x:Name="RootGrid" Background="{ThemeResource AppBackgroundBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Border x:Name="TopStatusStrip" Grid.Row="0" Padding="16,10" Background="{ThemeResource CardBackgroundBrush}">
      <Grid ColumnDefinitions="*,Auto">
        <TextBlock FontWeight="SemiBold" Text="SonicRelay Publisher" />
        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
          <FontIcon FontSize="10" Glyph="&#xE946;" />
          <TextBlock Style="{StaticResource MockStatusTextStyle}" Text="Mock state · Services not connected" />
        </StackPanel>
      </Grid>
    </Border>
    <NavigationView x:Name="ShellNavigation" Grid.Row="1" IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="False" PaneDisplayMode="LeftCompact"
                    SelectionChanged="OnNavigationSelectionChanged">
      <NavigationView.MenuItems>
        <NavigationViewItem Content="Dashboard" Icon="Home" Tag="Dashboard" />
        <NavigationViewItem Content="Connection" Icon="World" Tag="Connection" />
        <NavigationViewItem Content="Session" Icon="People" Tag="Session" />
        <NavigationViewItem Content="Audio" Icon="Volume" Tag="Audio" />
        <NavigationViewItem Content="Diagnostics" Icon="Repair" Tag="Diagnostics" />
        <NavigationViewItem Content="Settings" Icon="Setting" Tag="Settings" />
      </NavigationView.MenuItems>
      <Frame x:Name="ContentFrame" />
    </NavigationView>
    <Border x:Name="LogPanel" Grid.Row="2" Padding="16,10" Background="{ThemeResource CardBackgroundBrush}">
      <TextBlock Style="{StaticResource MockStatusTextStyle}" Text="Activity log · No events yet (placeholder)" />
    </Border>
  </Grid>
</Window>
